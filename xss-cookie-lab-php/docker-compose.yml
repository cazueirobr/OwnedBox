services:
  xss-cookie-lab-php:
    build: .
    ports:
      - "${LAB_PORT:-5000}:80"
    environment:
      FLAG_COOKIE: ${FLAG_COOKIE:-default-xss-flag}
      FLAG_COOKIE_NAME: ${FLAG_COOKIE_NAME:-flag}
